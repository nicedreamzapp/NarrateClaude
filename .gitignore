.DS_Store
.claude/

# Compiled Swift listener — users build this locally with `dictation setup`
dictation/bin/listen

# Runtime state — PID files, logs, window-id bindings
dictation/state/

# Backup files
*.bak
*.orig
*~

# Logs
*.log
*.log.*

# Swift build products and vendored checkouts
# dictation-v2 is a 7.5 GB local working tree (1,794 training wavs, Swift
# build products, vendored deps). Its source deserves its own repo, not this one.
dictation-v2/

# virtualenvs anywhere
**/.venv/
**/__pycache__/
